"""Curated design knowledge injected into the agents.

Holds shared thresholds for the design aware analysis tools and a concise block
of research informed design guidance that is injected into the Triage and Critic
instructions, so the agents reason with real level design knowledge rather than
the model's training data alone. Citations live in the report.

Implemented in Phase 4.
"""

from __future__ import annotations
